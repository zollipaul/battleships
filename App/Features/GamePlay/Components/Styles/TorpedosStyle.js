import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  shootContainer: {
    position: 'absolute',
    // left: 0,
    // right: 0,
    top: 20,
    // bottom: 0,
    flexDirection: 'row',
    justifyContent: 'center',
  },
})
